import React from 'react';
import CustomCarousel from "../components/Carousel";

const Home: React.FC = () => {
    return (
        <div>
            <CustomCarousel />
        </div>
    );
};

export default Home;
